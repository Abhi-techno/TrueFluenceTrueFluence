"use client";

import React from 'react';

const Footer = () => {
  // Footer is hidden for mobile-only app, but kept for potential future use.
  return (
    <footer className="hidden">
    </footer>
  );
};

export default Footer;
